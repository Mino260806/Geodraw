import 'package:flutter/material.dart';

class PaintTheme {
  final Paint drawPaint;
  final Paint shadowPaint;
  final TextStyle textStyle;

  PaintTheme(this.drawPaint, this.shadowPaint, this.textStyle);
}